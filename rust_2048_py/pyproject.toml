[build-system]
requires = ["maturin>=1.8"]
build-backend = "maturin"

[project]
name = "rust-2048-py"
version = "0.1.0"
requires-python = ">=3.13"

[tool.maturin]
features = ["pyo3/extension-module"]
